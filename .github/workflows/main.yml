name: Prevent Direct Push to Main

on:
  push:
    branches:
      - main

jobs:
  block_direct_push:
    runs-on: ubuntu-latest
    steps:
      - name: Prevent direct push
        run: |
          echo "❌ Direct push to main is not allowed!"
          exit 1  # إجبار GitHub على إلغاء العملية
