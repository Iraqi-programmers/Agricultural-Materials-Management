<Page x:Class="Interface.Pages.Ledger"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Interface.Pages.UserControl"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Ledger"
        FontFamily="Poppins">

    <!-- تعريف الانيميشن كموارد -->
    <Page.Resources>
        <!-- Storyboard لتكبير العنصر -->
        <Storyboard x:Key="MouseEnterAnimation">
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                             To="1.1" Duration="0:0:0.2" />
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                             To="1.1" Duration="0:0:0.2" />
            <ColorAnimation Storyboard.TargetProperty="(Panel.Background).(SolidColorBrush.Color)"
                            To="Transparent" Duration="0:0:0.2" />
        </Storyboard>

        <!-- Storyboard لإعادة العنصر إلى حالته الأصلية -->
        <Storyboard x:Key="MouseLeaveAnimation">
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                             To="1" Duration="0:0:0.2" />
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                             To="1" Duration="0:0:0.2" />
            <ColorAnimation Storyboard.TargetProperty="(Panel.Background).(SolidColorBrush.Color)"
                            To="Transparent" Duration="0:0:0.2" />
        </Storyboard>
    </Page.Resources>



    <Grid  Background="{DynamicResource SecundaryBackgroundColor}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition  Width="*"/>
            <ColumnDefinition  Width="*"/>

        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--المشتريات-->
        <Border
         Name="Purchases"
         Grid.Column="0"
         CornerRadius="20"
         Margin="7,4,7,0"
         Height="150"
         VerticalAlignment="Top"
         Background="{DynamicResource TertiaryBackgroundColor}"
         Grid.Row="0"
         Grid.RowSpan="1">

            <Grid>
                <local:BorderAnimation Text="المشتريات"
                                     TextColor="{DynamicResource TertiaryTextColor}"
                                     BackgroundColor="Transparent"
                                     IconSource="/Assets/R.jpg"
                                     Margin="0,0,0,0"
                                       
                                       />

            </Grid>
        </Border>

        <!--المبيعات-->
        <Border
         Name="Sales"
         Grid.Column="1"
         CornerRadius="20"
         Margin="7,4,7,0"
         Height="150"
         VerticalAlignment="Top"
         Background="{DynamicResource TertiaryBackgroundColor}"
         Grid.Row="0"
         Grid.RowSpan="1">

            <Grid>
                <local:BorderAnimation Text="المبيعات"
                                     TextColor="{DynamicResource TertiaryTextColor}"
                                     BackgroundColor="Transparent"
                                     IconSource="/Assets/R.jpg"
                                     Margin="0,0,0,0"
                                       
                                       />

            </Grid>
        </Border>


        <!--الديون-->
        <Border
         Name="Debts"
         Grid.Column="2"
         CornerRadius="20"
         Margin="7,4,7,0"
         Height="150"
         VerticalAlignment="Top"
         Background="{DynamicResource TertiaryBackgroundColor}"
         Grid.Row="0"
         Grid.RowSpan="2">

            <Grid>
                <local:BorderAnimation Text="الديون"
                                     TextColor="{DynamicResource TertiaryTextColor}"
                                     BackgroundColor="Transparent"
                                     IconSource="/Assets/R.jpg"
                                     Margin="0,0,0,0"
                                       
                                       />

            </Grid>
        </Border>

        <!--الإيرادات-->
        <Border
         Name="Revenue"
         Grid.Column="2"
         CornerRadius="20"
         Margin="7,-40,7,0"
         Height="150"
         VerticalAlignment="Top"
         Background="{DynamicResource TertiaryBackgroundColor}"
         Grid.Row="1"
         Grid.RowSpan="2">

            <Grid>
                <local:BorderAnimation Text="الإيرادات"
                                     TextColor="{DynamicResource TertiaryTextColor}"
                                     BackgroundColor="Transparent"
                                     IconSource="/Assets/R.jpg"
                                     Margin="0,-7,0,0"
                                       MouseDown="BorderAnimation_MouseDown"
                                       
                                       />

            </Grid>
        </Border>



    </Grid>


</Page>
