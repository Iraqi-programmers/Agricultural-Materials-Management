<Page x:Class="Interface.Pages.AccountingDepartment.ViewPurchasesList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Interface.Pages.AccountingDepartment"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ViewPurchasesList">

    <Grid>
       

        <DataGrid  x:Name="PurchasesDataGrid"
                      AutoGenerateColumns="False"
                      HeadersVisibility="Column"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      GridLinesVisibility="Vertical"
                      RowBackground="{DynamicResource PrimaryBackgroundColor}"
                      AlternatingRowBackground="{DynamicResource PrimaryBackgroundColor}"
                      BorderThickness="1"
                      Margin="5,5,5,0"
                      FontSize="20"
                     
                     >

            <DataGrid.Columns>
                <DataGridTextColumn Header="رقم القائمة" Width="*"></DataGridTextColumn>
                <DataGridTextColumn Header="المبلغ الكلي" Width="*"></DataGridTextColumn>
                <DataGridTextColumn Header="المبلغ المدفوع" Width="*"></DataGridTextColumn>
                <DataGridTextColumn Header="التاريخ" Width="*"></DataGridTextColumn>
            </DataGrid.Columns>
            
        </DataGrid>

    </Grid>
        
</Page>
