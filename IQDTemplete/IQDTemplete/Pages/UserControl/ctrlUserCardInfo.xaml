<UserControl x:Class="Interface.Pages.UserControl.ctrlUserCardInfo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Interface.Pages.UserControl"
             mc:Ignorable="d" 
         d:DesignHeight="600" d:DesignWidth="500"
             FlowDirection="RightToLeft">
    
    <Border Background="#FFF0F0F0" CornerRadius="10" Padding="15" BorderBrush="#FFCCCCCC" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- PersonCardInfo -->
            <local:ctrlPersonCardInfo x:Name="PersonInfoControl" Grid.Row="0" Margin="0,0,0,5"/>

            <!-- العنوان -->
            <TextBlock Text="معلومات المستخدم" Grid.Row="1" Grid.Column="0" 
                       VerticalAlignment="Center" HorizontalAlignment="Center" 
                       Margin="0,0,0,10" FontSize="18" FontWeight="Bold" Foreground="#FF0078D7"/>

            <!-- اسم المستخدم -->
            <TextBlock Text="اسم المستخدم:" Grid.Row="2"  VerticalAlignment="Center" Margin="5" FontWeight="SemiBold"/>
            <TextBox x:Name="txtUsername" Grid.Row="2"  Margin="0,5,0,5" Padding="5" Width="200" Background="White" BorderBrush="#FFCCCCCC" BorderThickness="1"/>

            <!-- كلمة السر -->
            <TextBlock Text="كلمة السر:" Grid.Row="3" VerticalAlignment="Center" Margin="5" FontWeight="SemiBold"/>
            <PasswordBox x:Name="txtPassword" Grid.Row="3"  Margin="0,5,0,5" Width="200" Padding="5" Background="White" BorderBrush="#FFCCCCCC" BorderThickness="1"/>

            <!-- حالة الحساب -->
            <StackPanel Grid.Row="4" Orientation="Horizontal" VerticalAlignment="Center" Margin="5">
                <TextBlock Text="حالة الحساب:" Grid.Row="4"  VerticalAlignment="Center" HorizontalAlignment="Left" Margin="5" FontWeight="SemiBold"/>
                <CheckBox x:Name="chkIsActive" VerticalAlignment="Center"  Margin="10,0,0,0" />
            </StackPanel>

            
            <!-- Save Button -->
            <StackPanel Grid.Row="6"  Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Stretch" >
                <Button x:Name="btnSave" Grid.Row="6"  Content="حفظ" Style="{StaticResource ModernButtonStyle}" Width="120" Height="35" Visibility="{Binding visibalBtnSave}" Click="btnSave_Click"/>
                <Button x:Name="btnEdit" Grid.Row="6"  Content="تعديل" Style="{StaticResource ModernButtonStyle}" Width="120" Height="35" Visibility="{Binding visibalBtnEdit}" Click="btnEdit_Click"/>
            </StackPanel>

        </Grid>
    </Border>
</UserControl>
